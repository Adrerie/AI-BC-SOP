# Benchmark

This directory contains benchmark specifications for trustworthy and reliable machine learning evaluation.

The benchmark framework will cover IID performance, distribution shift, calibration, uncertainty, selective prediction, robustness and failure analysis, with optional evaluation of explainability and computational cost.
