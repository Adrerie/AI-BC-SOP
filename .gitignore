# Local audit working area: scripts plus bulk per-heading text extracts of the source PDF.
# Kept out of the repository on purpose — the extracts are verbatim book text, while the
# committed artifacts under Validation/ carry only the short, cited notes actually needed.
_audit/
# Local working area for the committed validation tools: rebuilt indexes and caches.
# Citation indices are derived from a file that is not in this repository, so they are
# regenerable per machine; the committed copy is the reviewed one.
Validation/Trustworthy-ML-2023/tools/.cache/
__pycache__/
*.pyc
